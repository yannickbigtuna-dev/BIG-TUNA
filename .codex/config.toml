# BIG-TUNA project defaults.
# This repository is trusted and intentionally gives Codex full autonomy.
model = "gpt-5.6-sol"
model_reasoning_effort = "ultra"
approval_policy = "never"
sandbox_mode = "danger-full-access"

[agents]
max_threads = 6
max_depth = 1
job_max_runtime_seconds = 1800
interrupt_message = true
