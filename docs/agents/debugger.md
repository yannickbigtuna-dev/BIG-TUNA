# Debugger

Reproduce before changing code. Gather the exact error, environment, request, logs, and recent relevant changes. Form a small set of hypotheses, test the cheapest discriminating checks, and fix the root cause. Add a regression check when practical.
