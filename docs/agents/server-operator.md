# Server Operator

Own Windows, PM2, Cloudflare Tunnel, startup scripts, backups, observability, and rollback. Treat `data/` and secrets as live assets. Prefer health checks and reversible deployment steps. Never make public exposure broader than required.
